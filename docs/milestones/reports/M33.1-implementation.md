# Milestone Implementation Report

## Milestone

- Milestone: M33.1 — Table Flow & Mobile Polish (patch release `v1.1.1`)
- Branch: `milestone-33.1-table-flow-mobile-polish`
- Implementer: Claude Code
- Final HEAD: the commit adding this report (base `d1957f9`, the versioned specification)

## Verification

- `npm run verify`: passed (exit 0) on the final M33.1 working tree, report included
- Tests: Vitest 44 files / 710 tests passed (702 before); Playwright Chromium 51/51 passed (41 existing + 9 `table-layout.spec.ts` + 1 release-metadata test)
- Build: `tsc -b && vite build` passed (part of `npm run verify`)
- `git diff --check`: passed (part of `npm run verify`)
- Working tree at completion: clean

Environment note (as in M30–M33): the container ships Chromium build 1194 while the pinned
`@playwright/test` 1.63 expects headless-shell build 1243. The unchanged repository command
ran after a container-only symlink to the preinstalled 1194 headless shell; no repository
change. CI installs its own matching browser. The container Node is 22.22.2 (the
`engines` field asks for ≥22.23.2); npm only warned.

## Behaviour implemented

### Seating and turn flow (AC1, AC2)

- `tableSeats` (`GameTable.tsx`) maps from the existing player order and `teamId`: the
  opponent right after the human on the left, the teammate on top, the remaining opponent
  on the right. Seats render in clockwise document order (left, top, right). No domain
  state changes.
- Active turn: the active seat is gold with a steady halo (animated only without reduced
  motion), a filled avatar and the existing «Di turno» text/`aria-current`; the human's
  hand area gets the same emphasis (`active-player--turn`) on the human's turn. The polite
  turn banner is unchanged and stays authoritative.

### Melds as board state (AC3–AC5)

- `.meld-list` has no max-height/overflow and `.meld__cards` wraps instead of scrolling.
- `meldDensity` (`MeldArea.tsx`) picks `roomy` / `compact` / `dense` per team from public
  counts only (≥7 melds or ≥36 cards → dense; ≥4 melds, ≥18 cards or a ≥10-card meld →
  compact). CSS custom properties per density set tile width, card size, overlap strip,
  pip and corner size. A meld of ≥8 cards spans a whole grid row. Overlap is sized so each
  card's rank/suit corner stays exposed; annotated (wildcard/pinella) cards are wider and
  never covered.
- Narrow meld tiles (container query `meld`): label and extension control share row 1,
  the Burraco badge gets its own row, cards span the tile. The visible extension label
  shortens to «Aggiungi» (≤16rem) or «＋» (≤9.5rem); the accessible name
  «Aggiungi alla calata N della squadra X» and the 44 px target are unchanged. In dense
  areas the type word («Sequenza»/«Combinazione») is visually hidden but stays in the DOM.
- Wide desktop: the table uses `min-height` instead of a fixed height, so a very full
  board grows the page rather than hiding melds.

### Followable bot choreography (AC6, AC7)

- `botPlaybackDelay(speed, feedback)` is the single delay function. Normal: 900 ms
  ordinary (`BOT_PLAYBACK_DELAYS_MS.normal`), 1200 ms (`BOT_SIGNIFICANT_STEP_DELAYS_MS`)
  after a meld play/extension, discard or player hand-off, newly reached/changed Burraco
  or pozzetto acquisition — classified only from the committed `TableFeedback` cue. The
  human's discard is a hand-off, so the first bot action waits 1200 ms; a mounted,
  restored or fresh-round pending bot waits 900 ms. Fast stays 150 ms. `Completa subito`,
  speed-change rescheduling and session-replacement cancellation are unchanged (same
  effect/cleanup path). Bot flights stay ≤380 ms, within every normal delay.

### Active-table polish (AC8)

- Lighter chrome: meld areas are soft felt zones (no framed border unless active), the
  hand area has a neutral border except on the human's turn, lighter outer table shadow
  on mobile.
- ≤760 px: one-line meld headers, compact turn banner, smaller/dimmer persistent guidance
  and «Nuova calata» help text, tighter hub and hand padding. ≤440 px: smaller stock
  artwork. Settings/Help/history/«Nuova partita» are unchanged and keep 44 px targets.

### Release patch (AC11)

- `package.json` / root `package-lock.json` → `1.1.1`; README current release `v1.1.1`;
  `docs/RELEASE.md` gains the v1.1.1 row, the patch exact-SHA gate note and v1.1.1
  checklist examples. `docs/ARCHITECTURE.md` documents seat mapping, adaptive meld
  presentation and variable cadence. `docs/RULES.md` untouched.

## Tests added or changed

- New `src/tests/denseTable.ts`: save-valid stress table (7 melds per team, two 12-card
  wildcard sequences, clean and dirty Burraco, a pinella, both pozzetti taken, human in
  action phase holding cards that extend two own melds).
- New `src/components/MeldArea.test.tsx`: density thresholds; dense area renders every
  meld and card (identity, annotations, Burraco text, long-meld row), extension control.
- `GameTable.test.tsx`: new seat mapping and clockwise DOM order; active emphasis moves
  human → North (left) → Partner (top) → … → human with exactly one active place.
- `GameTablePlayback.test.tsx`: delay mapping values; per-step normal cadence checked
  against an independent public-fact classifier; explicit hand-off → draw (900) → meld
  (1200) → discard/hand-off (1200) sequence; pozzetto acquisition (1200); fast and
  immediate completion with no stale timer after unmount; speed change rescheduling a
  pending hand-off step.
- Existing timing tests adapted to the variable cadence (one-step helpers advance the
  significant delay, which is shorter than two ordinary delays; exact-boundary tests use
  the precise expected delay).
- New `e2e/table-layout.spec.ts` at 320, 390, 768, 1440 px: no document overflow; every
  meld/card visible; no `.meld-list`/`.meld__cards` scroll container or overflow; no
  covered rank/suit corner and no card outside its meld; Burraco/«Matta» text; 44 px
  primary/secondary controls and extension controls; keyboard extension and real pointer
  drag onto own melds. Desktop seat geometry (left < top < right, teammate above).
- `e2e/smoke.spec.ts`: first bot action does not commit at 1199 ms and commits at 1200 ms
  on the browser fake clock. `e2e/fixtures.ts`: `NORMAL_HANDOFF_DELAY_MS = 1200`.
- `e2e/deployment.spec.ts`: package/lock/README/RELEASE metadata agree on `1.1.1`.

## Viewport / manual inspection notes

Screenshots of the stress table were inspected at 320×740, 390×844, 768×1024, 1280×800
and 1440×900.

- 320 / 390: dense two-column meld tiles; 12-card sequences wrap onto a second row;
  every corner readable; no horizontal page scroll; the hand keeps its existing local
  horizontal scroll (unchanged, out of scope).
- 768: three-column tiles; long melds full row.
- 1280 / 1440: three-column table (own melds | hub | opponents' melds); with the stress
  board the table grows past the viewport height and the page scrolls vertically.

## Deviations from specification

None known.

## Known risks and ambiguities

- Residual density trade-off: at 320 px dense meld cards are 2.35×3.2 rem with a
  0.62 rem corner (above the existing 0.56 rem annotation minimum); long sequences wrap
  onto two rows. On wide desktop a stress board no longer fits one screen height: the
  page scrolls vertically instead of any meld list scrolling. No meld or card is hidden.
- The spec's hand-off list does not say whether the human's own discard counts; it is
  treated as a player hand-off (first bot action after the human waits 1200 ms, which
  also satisfies the ≥900 ms requirement).
- Collecting the discard pile is classified as an ordinary step (not in the spec's
  significant list).
- Narrow-tile layout uses CSS container queries; a browser without them falls back to
  the control beside the cards (no information lost).
- The active-seat halo is an infinite CSS animation; reduced motion collapses it through
  the existing global policy.

## Incidental changes

- `PlayerSeat.test.tsx` fixture positions updated to the new mapping.
- The 760 px override that enlarged meld cards on mobile was removed (replaced by density
  variables).

## Notes for independent review

- `botPlaybackDelay` / `isSignificantChange` in `GameTable.tsx` and the independent
  `isSignificantStep` in `GameTablePlayback.test.tsx`.
- `meldDensity` thresholds and the `.meld-area[data-density]` variables / container
  queries in `styles.css`.
- `e2e/table-layout.spec.ts` geometry assertions (corner coverage, no scroll container).
