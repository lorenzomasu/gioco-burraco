# Milestone Implementation Report

## Milestone

- Milestone: MXX
- Branch: `milestone-XX-name`
- Implementer: Codex / Claude Code / other

## Verification

- `npm run verify`: passed / failed / not run (for an internal checkpoint in an approved batch, use the batch report instead)
- Tests: <count/result>
- Build: passed / failed / not run
- `git diff --check`: passed / failed / not run
- Working tree at completion: clean / describe state

## Behaviour implemented

Briefly summarize the implemented milestone behaviour without restating the full specification.

## Deviations from specification

List every known deviation from the milestone specification.

Write `None` when there are no known deviations.

## Known risks and ambiguities

Record implementation risks, undefined or ambiguous cases, changed error timing/behaviour, assumptions, and anything the independent reviewer should inspect closely.

Write `None` when there are no known risks or ambiguities.

## Incidental changes

Record small changes that were not a primary milestone requirement but were introduced as part of the implementation.

Write `None` when there are no incidental changes.

## Notes for independent review

List specific areas, tests, edge cases, or mutation-sensitive boundaries that deserve focused review.

This report is advisory review context. It does not override the milestone specification, `docs/RULES.md`, `docs/ARCHITECTURE.md`, tests, or the implementation itself.
